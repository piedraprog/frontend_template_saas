/**
 * Sobrescrito en builds `development` (ver `angular.json` → fileReplacements).
 * Valores locales por defecto; `production` viene de `environment.ts`.
 */
export const environment = {
  production: false,
  apiUrl: 'http://localhost:3000',
  captcha_key: '1x00000000000000000000AA',
};
